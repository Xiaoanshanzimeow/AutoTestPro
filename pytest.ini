[pytest]
addopts= --reruns 2 --reruns-delay 1 -n auto --dist=loadscope
;失败后重试2次，每次重试之间等待1秒,按 CPU 核数自动起 N 个 pytest 进程，用例分发到不同进程并行跑。放在 addopts 里就是"默认就并行"。
;testpaths=./testcase

filterwarnings=
    error
    ignore::UserWarning

python_files=test_*.py

python_classes=Test*

python_functions=test